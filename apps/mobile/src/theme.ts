import { darkColors, spacing, typography, radius } from "@flare/design-tokens";

/** FLARE mobile defaults to the dark brand palette (see design-tokens/src/colors.ts). */
export const colors = darkColors;
export const space = spacing;
export const type = typography;
export const rad = radius;
